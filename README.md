# Pentagon Demo

A minimal Next.js demo app.

## Local development

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

## Deploy on Vercel

Push this repo to GitHub and import it in [Vercel](https://vercel.com). Vercel detects Next.js automatically and runs `npm run build`.
